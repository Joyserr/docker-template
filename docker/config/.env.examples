# ========================================
# Docker镜像配置文件模板
# ========================================
# 请复制此文件为 .env 并根据需要进行配置

# 用户配置（与主机用户保持一致，避免权限问题）
USER_NAME=your_username
USER_UID=1000
USER_GID=1000

# Docker镜像配置
IMAGE_NAME=my-ros-dev-image
IMAGE_TAG=latest
CONTAINER_NAME=my_ros_dev_container

# 工作空间目录（挂载到容器中的目录）
WORKSPACE_DIR=/path/to/your/workspace

# ========================================
# 可选配置（根据需要取消注释）
# ========================================

# 端口映射（示例）
# PORTS=8080:8080 3000:3000

# 环境变量（示例）
# ENV_VAR1=value1
# ENV_VAR2=value2

# GPU支持（需要nvidia-docker）
# GPU_SUPPORT=false

# ROS特定配置
ROS_MASTER_URI=http://localhost:11311
ROS_HOSTNAME=localhost

# GUI支持（用于RViz等图形界面）
ENABLE_GUI=true
DISPLAY=:0

# ROS工作空间
CATKIN_WS=~/catkin_ws

# 仿真环境支持
ENABLE_SIMULATION=false
SIMULATOR=Gazebo

# 构建配置
# 是否使用国内镜像源（加速构建）
USE_CN_MIRROR=true

# Docker 构建参数
BUILD_ARGS=--build-arg USE_CN_MIRROR=${USE_CN_MIRROR}