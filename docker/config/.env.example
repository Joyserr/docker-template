# Docker 开发环境配置
# 运行 'make init' 自动生成此文件，或复制为 .env 手动编辑

# 用户 (与主机一致，避免权限问题)
USER_NAME=user
USER_UID=1000
USER_GID=1000

# 镜像
IMAGE_NAME=dev
IMAGE_TAG=latest
CONTAINER_NAME=dev

# 工作空间
WORKSPACE_DIR=/path/to/your/project
CONTAINER_WORKSPACE=workspace
